<Window x:Class="Paftax.Pafta.UI.Views.ExportScheduleView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Paftax.Pafta.UI.Views"
        xmlns:vm="clr-namespace:Paftax.Pafta.UI.ViewModels" d:DataContext="{d:DesignInstance Type=vm:ExportScheduleViewModel}"
        mc:Ignorable="d"
        Title="ExportScheduleView" Height="700" Width="500"
        ResizeMode="NoResize">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="90"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>

        <TextBox x:Name="SearchTextBox" Grid.Row="0" HorizontalAlignment="Stretch" VerticalAlignment="Center" Margin="10" Height="25"/>

        <DataGrid x:Name="SchedulesDataGrid" Grid.Row="1" AutoGenerateColumns="False" Margin="10" ItemsSource="{Binding ScheduleViewModels}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Schedule Name" Binding="{Binding Name}" Width="*"/>
                <DataGridCheckBoxColumn Header="Select" Binding="{Binding IsSelected}" Width="Auto"/>
            </DataGrid.Columns>
        </DataGrid>

        <Grid Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="25"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Export Options:" FontWeight="Bold" Grid.Row="0" VerticalAlignment="Center" Margin="10,0,0,0" IsHitTestVisible="False"/>

            <StackPanel Orientation="Horizontal" Grid.Row="1" VerticalAlignment="Center" Margin="10,0,0,0">
                <TextBlock Text="Folder:" VerticalAlignment="Center" IsHitTestVisible="False"/>
                <TextBox x:Name="FolderTextBox" VerticalAlignment="Center" Margin="10,0,10,0" Height="25" Width="400"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Grid.Row="2" VerticalAlignment="Center" Margin="10,0,0,0">
                <RadioButton x:Name="MergedButton" Content="Merged" Margin="0,0,10,0"/>
                <RadioButton x:Name="SeperatedButton" Content="Seperated"/>
            </StackPanel>
        </Grid>

        <StackPanel Orientation="Horizontal" Grid.Row="4" HorizontalAlignment="Right" Margin="10" Height="25">
            <Button x:Name="ExportButton" Content="Export" Width="80" Margin="0,0,10,0"/>
            <Button x:Name="CancelButton" Content="Cancel" Width="80"/>
        </StackPanel>
    </Grid>
</Window>
