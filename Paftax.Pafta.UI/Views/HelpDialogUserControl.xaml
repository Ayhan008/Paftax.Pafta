<UserControl x:Class="Paftax.Pafta.UI.Views.HelpDialogUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:Paftax.Pafta.UI.Views"
             xmlns:vm="clr-namespace:Paftax.Pafta.UI.ViewModels"
             xmlns:controls="clr-namespace:Paftax.Pafta.UI.Controls"
             mc:Ignorable="d"
             d:DesignHeight="250" d:DesignWidth="400">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Paftax.Pafta.UI;component/Resources/Generic.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="70"/>
        </Grid.RowDefinitions>

        <Border HorizontalAlignment="Stretch" Grid.Row="0" Height="1" VerticalAlignment="Top"/>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="50"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <controls:FontIcon Grid.Column="0"
                                   Margin="10"
                                   Glyph="&#xe897;"                           
                                   FontSize="40"
                                   VerticalAlignment="Top"/>

            <TextBlock x:Name="HelpTextBlock"
                        Grid.Column="1"
                        Margin="10,10,10,10"
                        TextWrapping="Wrap"
                        TextTrimming="CharacterEllipsis"
                        Width="330"
                        VerticalAlignment="Top"
                        HorizontalAlignment="Left"
                        FontWeight="{StaticResource FontWeightBold}"
                        FontSize="{StaticResource FontSizeSmall}"
                        Foreground="{DynamicResource WindowForeground}"/>
        </Grid>

        <Border HorizontalAlignment="Stretch" Grid.Row="0" Height="1" VerticalAlignment="Bottom"/>

        <Border Grid.Row="1" Background="{DynamicResource DataGridColumnHeaderBackground}" CornerRadius="0,0,9.5,9.5">
            <Button x:Name="OkButton" 
                        Background="{DynamicResource DataGridColumnHeaderBackground}"
                        Grid.Row="2"
                        Content="OK"
                        Width="80"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Bottom"
                        Margin="0,0,10,10"
                        Command="{Binding OkCommand}"/>
        </Border>

    </Grid>
</UserControl>
