<UserControl x:Class="Paftax.Pafta.UI.Views.CleanGarbageUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Paftax.Pafta.UI.Views"
             xmlns:properties="clr-namespace:Paftax.Pafta.UI.AttachedProperties" properties:ThemeChanged.EnableTheme="True"           
             xmlns:vm="clr-namespace:Paftax.Pafta.UI.ViewModels"
             xmlns:converters="clr-namespace:Paftax.Pafta.UI.Converters"
             xmlns:controls="clr-namespace:Paftax.Pafta.UI.Controls" 
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=vm:CleanGarbageViewModel}"
             d:DesignHeight="800" 
             d:DesignWidth="450">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Paftax.Pafta.UI;component/Resources/Generic.xaml"/>
                <ResourceDictionary Source="/Paftax.Pafta.UI;component/Resources/Styles/FontStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <converters:BoolToVisibility x:Key="BoolToVisibility"/>
        </ResourceDictionary>
    </UserControl.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="45"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" 
                HorizontalAlignment="Stretch" 
                Height="1"
                VerticalAlignment="Top"/>

        <TabControl Grid.Row="0" Margin="10" SnapsToDevicePixels="True"
                    SelectedIndex="{Binding SelectedTabIndex, Mode=TwoWay}">
            <TabItem Header="Views">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="50"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0" Margin="10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <ComboBox x:Name="ViewsComboBox"
                                  SelectedIndex="{Binding SelectedViewFilterIndex, Mode=TwoWay}"
                                  ItemsSource="{Binding ViewFilters}"
                                  Grid.Column="0"                             
                                  BorderThickness="1"
                                  HorizontalAlignment="Left"
                                  VerticalAlignment="Center"
                                  Height="30"
                                  Width="100"/>

                        <controls:SearchBox VerticalAlignment="Center"
                                            Grid.Column="1"
                                            HorizontalAlignment="Stretch" 
                                            Margin="10,0,0,0" 
                                            Height="30"
                                            PlaceholderText="Search for a View..."
                                            SearchText="{Binding ViewSearchText, UpdateSourceTrigger=PropertyChanged}"/>
                    </Grid>

                    <DataGrid x:Name="ViewsDataGrid" 
                              Grid.Row="1" 
                              AutoGenerateColumns="False" 
                              Margin="10,0,10,10" 
                              EnableRowVirtualization="True"
                              EnableColumnVirtualization="True"
                              properties:MutipleCheck.IsEnabled="True"
                              ItemsSource="{Binding ViewsView}">
                        <DataGrid.Columns>
                            <DataGridTemplateColumn Width="30">
                                <DataGridTemplateColumn.Header>
                                    <CheckBox x:Name="SelectAllCheckBox" 
                                              IsChecked="{Binding DataContext.IsAllViewsChecked, Mode=TwoWay, RelativeSource={RelativeSource AncestorType=DataGrid}}"/>
                                </DataGridTemplateColumn.Header>
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <CheckBox IsChecked="{Binding IsChecked, Mode=TwoWay}" 
                                                  Foreground="{DynamicResource DataGridForeground}"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTemplateColumn Header="Name" Width="*" SortMemberPath="Name">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <controls:FontIcon Glyph="{Binding IsPlacedGlyph}" Background="Transparent" FontSize="15"/>
                                            <TextBlock Text="{Binding Name}" Margin="10,0,0,0"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTextColumn Header="View Type" 
                                                Foreground="{DynamicResource DataGridForeground}"
                                                Binding="{Binding ViewType}" 
                                                Width="100"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>

            <TabItem Header="View Templates">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="50"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0" Margin="10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <ComboBox x:Name="ViewTemplatesComboBox"
                                  Grid.Column="0"
                                  BorderThickness="1"
                                  SelectedIndex="{Binding SelectedViewTemplateFilterIndex, Mode=TwoWay}"
                                  ItemsSource="{Binding ViewTemplateFilters}"
                                  HorizontalAlignment="Left"
                                  VerticalAlignment="Center"
                                  Height="30"
                                  Width="100"/>


                        <controls:SearchBox VerticalAlignment="Center"
                                        Grid.Column="1"
                                        HorizontalAlignment="Stretch" 
                                        Margin="10,0,0,0" Height="30"
                                        PlaceholderText="Search for a View Template..."
                                        SearchText="{Binding ViewTemplateSearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                    </Grid>
                    
                    <DataGrid Grid.Row="1" 
                              AutoGenerateColumns="False" 
                              Margin="10,0,10,10" 
                              EnableRowVirtualization="True"
                              EnableColumnVirtualization="True"
                              properties:MutipleCheck.IsEnabled="True"
                              ItemsSource="{Binding ViewTemplates}">
                        <DataGrid.Columns>
                            <DataGridTemplateColumn Width="30">
                                <DataGridTemplateColumn.HeaderTemplate>
                                    <DataTemplate>
                                        <CheckBox IsChecked="{Binding DataContext.IsAllViewTemplatesChecked, RelativeSource={RelativeSource AncestorType=DataGrid}, Mode=TwoWay}" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.HeaderTemplate>
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <CheckBox IsChecked="{Binding IsChecked, Mode=TwoWay}" 
                                                  Foreground="{DynamicResource DataGridForeground}"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTextColumn Header="Name" 
                                                Foreground="{DynamicResource DataGridForeground}"
                                                Binding="{Binding Name}" 
                                                Width="*"/>
                            <DataGridTextColumn Header="View Count" 
                                                Foreground="{DynamicResource DataGridForeground}"
                                                Binding="{Binding ViewCount}" 
                                                Width="100"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>

            <TabItem Header="Filters">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="50"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0" Margin="10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <ComboBox x:Name="FiltersTypeComboBox"
                                  SelectedIndex="{Binding SelectedFilterFilterIndex, Mode=TwoWay}"
                                  ItemsSource="{Binding FilterFilters}"
                                  Grid.Column="0"
                                  BorderThickness="1"
                                  HorizontalAlignment="Left"
                                  VerticalAlignment="Center"
                                  Height="30"
                                  Width="100"/>

                        <controls:SearchBox VerticalAlignment="Top"
                                            Grid.Column="1"
                                            HorizontalAlignment="Stretch" 
                                            Margin="10,0,0,0" Height="30"
                                            PlaceholderText="Search for a Filter..."
                                            SearchText="{Binding FilterSearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                    </Grid>

                    <DataGrid Grid.Row="1" 
                              AutoGenerateColumns="False" 
                              Margin="10,0,10,10" 
                              EnableRowVirtualization="True"
                              EnableColumnVirtualization="True"
                              properties:MutipleCheck.IsEnabled="True"
                              ItemsSource="{Binding Filters}">
                        <DataGrid.Columns>
                            <DataGridTemplateColumn Width="30">
                                <DataGridTemplateColumn.HeaderTemplate>
                                    <DataTemplate>
                                        <CheckBox IsChecked="{Binding DataContext.IsAllFiltersChecked, RelativeSource={RelativeSource AncestorType=DataGrid}, Mode=TwoWay}" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.HeaderTemplate>
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <CheckBox IsChecked="{Binding IsChecked, Mode=TwoWay}" 
                                                  Foreground="{DynamicResource DataGridForeground}"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTextColumn Header="Name" 
                                                Foreground="{DynamicResource DataGridForeground}"
                                                Binding="{Binding Name}" 
                                                Width="*"/>
                            <DataGridTextColumn Header="Template Count" 
                                                Foreground="{DynamicResource DataGridForeground}"
                                                Binding="{Binding TemplateCount}" 
                                                Width="100"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <Grid Grid.Row="1" 
                          HorizontalAlignment="Center" 
                          VerticalAlignment="Top" 
                          Margin="0,250,0,0"
                          Visibility="{Binding FiltersLoaded, Converter={StaticResource BoolToVisibility}}">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <controls:ProgressRing Width="80" Height="80"
                                               Grid.Row="0"
                                               HorizontalAlignment="Center" 
                                               StrokeThickness="6"
                                               Background="{DynamicResource DataGridColumnHeaderBackground}"
                                               Foreground="{DynamicResource DataGridForeground}"
                                               IsIndeterminate="True"
                                               VerticalAlignment="Center"/>

                        <TextBlock Grid.Row="1" IsHitTestVisible="False" 
                                   Text="{Binding FiltersLoadingStatusText}" 
                                   FontFamily="{StaticResource PrimaryFont}"
                                   FontWeight="{StaticResource FontWeightNormal}"
                                   FontSize="{StaticResource FontSizeMedium}"
                                   HorizontalAlignment="Center" 
                                   VerticalAlignment="Bottom"
                                   Margin="0,10,0,0"
                                   Foreground="{DynamicResource DataGridForeground}"/>
                    </Grid>
                </Grid>
            </TabItem>
            <TabItem Header="Materials">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="50"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0" Margin="10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <ComboBox x:Name="MaterialsComboBox"
                                  SelectedIndex="{Binding SelectedMaterialFilterIndex, Mode=TwoWay}"
                                  ItemsSource="{Binding MaterialFilters}"
                                  Grid.Column="0"
                                  BorderThickness="1"
                                  HorizontalAlignment="Left"
                                  VerticalAlignment="Center"
                                  Height="30"
                                  Width="100"/>


                        <controls:SearchBox VerticalAlignment="Top"
                                            Grid.Column="1"
                                            HorizontalAlignment="Stretch" 
                                            Margin="10,0,0,0" Height="30"
                                            PlaceholderText="Search for a Material..."
                                            SearchText="{Binding MaterialSearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                    </Grid>

                    <DataGrid Grid.Row="1" 
                              AutoGenerateColumns="False" 
                              Margin="10,0,10,10" 
                              EnableRowVirtualization="True"
                              EnableColumnVirtualization="True"
                              properties:MutipleCheck.IsEnabled="True"
                              ItemsSource="{Binding Materials}">
                        <DataGrid.Columns>
                            <DataGridTemplateColumn Width="30">
                                <DataGridTemplateColumn.HeaderTemplate>
                                    <DataTemplate>
                                        <CheckBox IsChecked="{Binding DataContext.IsAllMaterialsChecked, RelativeSource={RelativeSource AncestorType=DataGrid}, Mode=TwoWay}" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.HeaderTemplate>
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <CheckBox IsChecked="{Binding IsChecked, Mode=TwoWay}" 
                                                  Foreground="{DynamicResource DataGridForeground}"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTextColumn Header="Name" 
                                                Foreground="{DynamicResource DataGridForeground}"
                                                Binding="{Binding Name}" 
                                                Width="*"/>
                            <DataGridTextColumn Header="Type Count" 
                                                Foreground="{DynamicResource DataGridForeground}"
                                                Binding="{Binding Count}" 
                                                Width="100"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <Grid Grid.Row="1" 
                          HorizontalAlignment="Center" 
                          VerticalAlignment="Top"
                          Margin="0,250,0,0"
                          Visibility="{Binding MaterialsLoaded, Converter={StaticResource BoolToVisibility}}">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <controls:ProgressRing Width="80" Height="80"
                                               Grid.Row="0"
                                               HorizontalAlignment="Center" 
                                               StrokeThickness="6"
                                               Background="{DynamicResource DataGridColumnHeaderBackground}"
                                               Foreground="{DynamicResource DataGridForeground}"
                                               IsIndeterminate="True"
                                               VerticalAlignment="Center"/>

                        <TextBlock Grid.Row="1" IsHitTestVisible="False"
                                   Text="{Binding MaterialsLoadingStatusText}"
                                   FontFamily="{StaticResource PrimaryFont}"
                                   FontWeight="{StaticResource FontWeightNormal}"
                                   FontSize="{StaticResource FontSizeMedium}"
                                   Margin="0,10,0,0"
                                   HorizontalAlignment="Center" 
                                   VerticalAlignment="Top"
                                   Foreground="{DynamicResource DataGridForeground}"/>
                    </Grid>
                </Grid>
            </TabItem>
            <TabItem Header="Lines">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="50"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0" Margin="10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <ComboBox x:Name="LinesComboBox"
                                  SelectedIndex="{Binding SelectedLineFilterIndex, Mode=TwoWay}"
                                  ItemsSource="{Binding LineFilters}"
                                  Grid.Column="0"
                                  BorderThickness="1"
                                  HorizontalAlignment="Left"
                                  VerticalAlignment="Center"
                                  Height="30"
                                  Width="100"/>


                        <controls:SearchBox VerticalAlignment="Top"
                                            Grid.Column="1"
                                            HorizontalAlignment="Stretch" 
                                            Margin="10,0,0,0" Height="30"
                                            PlaceholderText="Search for a Line..."
                                            SearchText="{Binding LineSearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                    </Grid>

                    <DataGrid Grid.Row="1" 
                              AutoGenerateColumns="False" 
                              Margin="10,0,10,10" 
                              EnableRowVirtualization="True"
                              EnableColumnVirtualization="True"
                              properties:MutipleCheck.IsEnabled="True"
                              ItemsSource="{Binding Lines}">
                        <DataGrid.Columns>
                            <DataGridTemplateColumn Width="30">
                                <DataGridTemplateColumn.HeaderTemplate>
                                    <DataTemplate>
                                        <CheckBox IsChecked="{Binding DataContext.IsAllLinesChecked, RelativeSource={RelativeSource AncestorType=DataGrid}, Mode=TwoWay}"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.HeaderTemplate>
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <CheckBox IsChecked="{Binding IsChecked, Mode=TwoWay}" 
                                                  Foreground="{DynamicResource DataGridForeground}"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTextColumn Header="Name" 
                                                Foreground="{DynamicResource DataGridForeground}"
                                                Binding="{Binding Name}" 
                                                Width="*"/>
                            <DataGridTextColumn Header="Instance Count" 
                                                Foreground="{DynamicResource DataGridForeground}"
                                                Binding="{Binding Count}" 
                                                Width="100"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <Grid Grid.Row="1" 
                          HorizontalAlignment="Center" 
                          VerticalAlignment="Top"
                          Margin="0,250,0,0"
                          Visibility="{Binding LinesLoaded, Converter={StaticResource BoolToVisibility}}">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <controls:ProgressRing Width="80" Height="80"
                                               Grid.Row="0"
                                               HorizontalAlignment="Center" 
                                               StrokeThickness="6"
                                               Background="{DynamicResource DataGridColumnHeaderBackground}"
                                               Foreground="{DynamicResource DataGridForeground}"
                                               IsIndeterminate="True"
                                               VerticalAlignment="Center"/>

                        <TextBlock Grid.Row="1" IsHitTestVisible="False" 
                                   Text="{Binding LinesLoadingStatusText}"
                                   FontFamily="{StaticResource PrimaryFont}"
                                   FontWeight="{StaticResource FontWeightNormal}"
                                   FontSize="{StaticResource FontSizeMedium}"
                                   TextAlignment="Center"
                                   Padding="0"                  
                                   LineHeight="20"
                                   HorizontalAlignment="Center" 
                                   Margin="0,10,0,0"
                                   VerticalAlignment="Top"
                                   Foreground="{DynamicResource DataGridForeground}"/>
                    </Grid>
                </Grid>
            </TabItem>
        </TabControl>

        <Border Grid.Row="0" 
                HorizontalAlignment="Stretch" 
                Height="1"
                VerticalAlignment="Bottom"/>

        <StackPanel Orientation="Horizontal" 
                    Margin="0,0,10,0"
                    Grid.Row="1" 
                    HorizontalAlignment="Right" 
                    VerticalAlignment="Center">
            
            <Button x:Name="CleanButton" Content="Clean" 
                    Grid.Row="2" 
                    HorizontalAlignment="Right" 
                    Margin="0,0,10,0"
                    VerticalAlignment="Center"
                    Style="{StaticResource ResultButtonStyle}"
                    Command="{Binding CleanCommand}"/>

            <Button x:Name="CancelButton" Content="Cancel" 
                    Grid.Row="2" 
                    HorizontalAlignment="Right" 
                    VerticalAlignment="Center"
                    Command="{Binding CancelCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>
