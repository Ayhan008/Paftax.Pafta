<Window x:Class="Paftax.Pafta.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Paftax.Pafta.UI"
        xmlns:views="clr-namespace:Paftax.Pafta.UI.Views"
        xmlns:vm="clr-namespace:Paftax.Pafta.UI.ViewModels"
        xmlns:behaviors="clr-namespace:Paftax.Pafta.UI.Behaviors"
        behaviors:ThemeChangedBehavior.EnableTheme="True"
        xmlns:controls="clr-namespace:Paftax.Pafta.UI.Controls" 
        d:DataContext="{d:DesignInstance Type=vm:MainWindowViewModel}"
        mc:Ignorable="d"
        AllowsTransparency="True"
        WindowStartupLocation="CenterScreen"
        Background="Transparent"
        WindowStyle="None"
        Title="MainWindow" Height="600" Width="700">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/PaftaxPaftaUI;component/Resources/Generic.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            
            <DataTemplate DataType="{x:Type vm:ExportScheduleViewModel}">
                <views:ExportScheduleView/>
            </DataTemplate>
        </ResourceDictionary>
    </Window.Resources>
    
    <Grid>
        <Border CornerRadius="20"
                Background="Black"
                BorderThickness="0"
                Opacity="0.010"/>

        <Border CornerRadius="15"
                Margin="5"
                Background="Black"
                BorderThickness="0"
                Opacity="0.015"/>

        <Border BorderBrush="{DynamicResource WindowBorderBrush}" 
                Background="{DynamicResource WindowBackground}" 
                Margin="10"
                BorderThickness="1"
                CornerRadius="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="30"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <controls:TitleBar x:Name="TitleBar"
                                   HorizontalAlignment="Stretch"
                                   Grid.Row="0"
                                   VerticalAlignment="Stretch"/>

                <ContentControl x:Name="DynamicContentControl"
                                Grid.Row="1" 
                                HorizontalAlignment="Stretch" 
                                VerticalAlignment="Stretch"
                                Content="{Binding CurrentViewModel}"/>
            </Grid>
        </Border>
    </Grid>
</Window>
